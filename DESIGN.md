# persona-suite 설계 기록

작성일: 2026-06-05
상태: 5 스킬 빌드 완료 — #1 `defining-requirements`·#2 `designing-data-model`·`validating-test-lists`(이동) 검증됨. #3 `designing-code` v1 (light 경로 dogfood 검증 / heavy 코어 미검증). `auditing-production-risks` v1 (단발 감사 경로 5회 검증·전 런 CRITICAL 0 / 재감사 루프 미검증).

개발 라이프사이클 단계별로 시니어 페르소나 티키타카를 돌려, **실제 피처를 정의/설계하면서 + 사용자가 프로세스를 체화**하는 스킬 모음. DB 스키마를 페르소나 핑퐁으로 문서화하던 선행 시스템에서 검증된 페르소나-핑퐁 패턴을, 라이프사이클 전반으로 확장한 것이다. (선행 사례: 저자의 한 프로젝트에서 운영하던 DB 스키마 문서화 페르소나 시스템 — 프로젝트 로컬 자산이라 이 플러그인은 그것에 의존하지 않고 *패턴만* 가져왔다.)

> 이식성 원칙: 이 플러그인은 cross-project / cross-machine 으로 배포된다. **특정 프로젝트의 경로·파일·수치를 플러그인 파일에 박지 않는다.** 그런 specifics 는 각 프로젝트의 `<repo>/.claude/persona-profile.md` 에만 산다.

## 1. 배경 / 문제

요구사항 정의는 "모호한 한 줄(원츠)"을 "검증 가능한 계약"으로 깎는 작업이다. 실무 가치는 (a) 기획을 현행 코드와 대조해 빈틈을 찾고, (b) 애매한 정책을 질문으로 못박고, (c) 기획서에 없는 예외·비기능 요구를 끌어내는 데서 난다. 설계 당시 저자의 스킬 생태계엔 이 단계를 강제하는 도구가 없었다 — 아이디어 탐색(greenfield)·구현 계획·테스트 리스트 검증 스킬은 있었지만, "피처는 대략 정해졌고 이제 요구사항을 엄밀히 정의"하는 단계가 비어 있었다.

## 2. 합의된 핵심 결정

| 항목 | 결정 | 근거 |
|---|---|---|
| 산출물 성격 | 실전 도구 + 학습 체화 (둘 다) | 실제 피처가 학습의 stakes를 주고 역할 페르소나가 문서 완성도를 줌 (상호 강화) |
| 메커니즘 | prompt-페르소나 티키타카 (한 대화, 컨텍스트 공유) | 티키타카는 턴 단위 상호작용 → 메인 컨텍스트 prompt-페르소나여야. subagent(독립 컨텍스트)면 교차 트리거가 깨짐 |
| 형태 | skill (process/flow는 skill의 본진) | 발견성(키워드 자동 표면화) + process 성격 |
| 응집/결합 | 단계=별도 skill(저결합, 핸드오프 문서가 인터페이스) / 페르소나=한 skill 내(고응집, 컨텍스트 공유·교차 트리거) | high cohesion, low coupling |
| 페르소나 존재 | 정의는 독립 블록 / 실행은 컨텍스트 공유 / 개별 호명 가능 | 모듈성 + 핑퐁 동시 확보 |
| 페르소나 출처 | 복사 ❌, 단일 출처 (겹치면 참조/모드확장) | 복사하면 두 출처가 drift |
| 공통 원칙 출처 | 복사 ❌ — root `PRINCIPLES.md` 단일 출처. SKILL.md/personas.md 엔 로드 지시 + 스킬 고유 변형만 | 동일 원칙이 6파일 7곳에 복제돼 수동 전파 drift 위험 → 2026-06-10 단일화 |
| 이식성 | 제네릭 엔진(플러그인) + 프로젝트 프로필(repo). 묻기 기본 + 프로필 자동 생성 | 묻기로 이식성 확보, 프로필은 재질문 생략 캐시 |
| 피드백 모드 | 협업 시니어 + 보정 (막혔을 때만 각도 제시) | 정책은 판단이라 순수 소크라테스보다 협업적, 단 떠먹이기 방지. 순수 Socratic 학습은 저자 환경의 별도 스킬(`architecture-interview`) 몫 — 이식성 위해 스킬 본문엔 고유명 비기재 |
| 배포 | 플러그인 (git repo, 크로스머신 install/version/update) | 다른 컴퓨터까지 사용 요구 → ~/.claude/skills 로컬은 안 따라감 |
| 감사(8.5) 실행 주체 | 독립 서브에이전트 단발 + 재감사 1회 상한 (메인 자기감사 ❌, 자동 수정 무한순환 ❌) | 격리는 작성자 편향(자기 코드 무죄 추정)·컨텍스트 혼동을 줄이지만 무근거 위반 주장은 못 줄임 → 근거 인용 의무(`파일:라인`) + 항목별 사용자 게이트로 방어. 무한 순환은 종료 조건이 LLM 판단에 떠서 false positive 에 끌려다님 (2026-06-10) |
| 체크리스트 성격 | 기준(카테고리 전부 판정 의무) + 앵커(항목은 본질의 예시) + 탈출구(`목록외` 라벨) 3층 | 기준만이면 천장 효과(목록 밖 못 봄), 가이드만이면 커버리지가 확률적. 완전성 대신 운영 사고 역주입으로 프로젝트 실제 분포에 수렴 (2026-06-10) |
| brainstorming graft (형태/분해 + 시각) | 형태/분해=게이트0 fold(generic) · 시각=`handoffs.visual` opt-in(superpowers 참조, 복제 ❌) · 어시스턴트 추천·spec-review 루프 미이식 | gap은 형태·시각 2개뿐(요구사항 깎기·대안비교·검증은 suite 우위). 분해=값싼 추론→플러그인 / 시각=인프라→핸드오프(재구현 금지). ④추천=§1(체화) 위반, spec-loop=§8(1회 bounded)이 우위라 의도적 제외 (2026-06-23) |
| 코드 대조 의무(§11 grounding) + 상류 reconciliation(§9) + 사후판정 solo 폴백 | `PRINCIPLES.md` §11 신설(코드 의존 출력 2종 = (부)존재·선례 주장 + 코드 샘플 충실성, 탐색 `src/` 한정 금지) · §8·§10 검증자가 §11 참조 · §9 에 상류/자매 문서 supersede 체크리스트(원칙만 — 고정 템플릿 필드는 보일러플레이트 우려로 미설치, 위치는 §9 가 지정) · `LIFECYCLE.md` 6a 핸드오프에 plan grounding 명령(인용한 코드만 비례 검증 — 이진 tier 판단·절벽 없음) · 요구사항 output-template §1 에 오너 부재 observe-only 폴백 | 한 무거움 tier 피처를 superpowers-only 경로 vs suite+grounded-plan 경로로 병렬 산출·비교(n=1): suite 가 정책·스키마 갭을, grounded 구현계획이 구현 CRITICAL(조회 반환형태 오인 → 전역 장애)을 잡음. superpowers 약점(grounding 없는 plan)과 suite 약점(fresh-context 검증자의 `src/`-한정 false-negative)이 **같은 뿌리=코드 대조 없이 단정** → 단일 원칙으로 흡수. 사후판정 게이트가 PM 존재를 가정해 solo 환경서 ❓ 영구 미해소 → observe-only 폴백 (2026-06-23) |
| 라이프사이클 갭 보강 (2026-08) — 역주입 큐 · 롤아웃/롤백 착지 · 관측/로깅 프로필화 · 스케일 경로 · 감사자 모델 핀 | **신규 스킬 0개** — 진짜 부재 단계는 13(회고·역주입) 하나뿐이고 그마저 큐로 해결. 나머지는 전부 *착지 필드* 신설로 흡수 | 갭을 "단계 부재 vs 착지 필드 부재"로 판별하니 5건 중 4건이 후자였다 — 페르소나가 압박은 하는데(`personas.md` 롤아웃/롤백 질문) 답이 들어갈 칸이 없어 문서에 안 남고 하류가 못 받는 유형. 단계를 늘리면 세리머니만 늘고 이 유형은 그대로 남는다. 13만 예외인 이유는 부재가 *구조적*(감사는 프로젝트 repo·checklist 는 플러그인 repo → 그 자리서 편입 불가)이라 필드로 못 메워서 — 그래서 큐(비동기 경계 잇기)이지 스킬이 아니다. 로깅·관측은 피처마다 재결정할 사안이 아니라 전역 관례라 프로필 1회 고정 + 감사가 준수 판정(핑퐁 세리머니 회피). 스케일 경로는 데이터모델 템플릿에만 흡수돼 있던 걸 3개 산출물로 형식 통일. **스케일 경로에 학습 모드 추가**: 사용자 목적이 "아키텍처 경험 확장"이라 기록 칸(예약 한 줄)만으론 미달 — 이름 하나는 경험이 되지 않는다. 단 학습 탐색을 문서에 실으면 하류 계약이 교재로 오염되므로 **대화=탐색 / 문서=압축**으로 청중을 갈랐고, no-pre-fill(먼저 묻고 나중에 보탬)로 §1 떠먹이기 금지와 정렬. 산출물 성격 첫 행("실전 도구 + 학습 체화")을 스케일 축에 뒤늦게 적용한 것 |

## 3. #1 `defining-requirements` 구성

- 페르소나 6종: `[PM]`①요구사항 · `[REV]`②기능정의(메인) · `[TECH]`③기술설계 · `[SEC]`횡단보안 · `[OPS]`운영 · `[QA]`검증.
- 플로우 게이트 5종: 스코프 슬라이싱(자기 완결 조각 분리 — tier 는 본체 기준) / right-size tier / 수정 시 AS-IS 코드 조사(일급 step) / 단방향+역류 / 종료 완성도 패스.
- 내용 소스: 실무 기능개발 사이클 방법론(①②③ 구분·단방향·NFR표·Saga·expand-contract·right-size)을 제네릭화해 흡수 (런타임 파일 의존 ❌ — 원본이 어느 프로젝트에 있든 플러그인은 내용만 내장).
- 상세는 `skills/defining-requirements/{SKILL.md, personas.md, output-template.md}`.

### 페르소나 6종이 된 이유 (gap 감사 결과)

`[OPS]` 는 완성도 감사에서 추가됨 — 산출물이 운영자·admin 개발자 청중인데 운영 렌즈가 없던 구멍. 그 외 보강: 지표 측정가능성(`[PM]`), 관측성 NFR(`[TECH]`), 종료 완성도 패스(`[REV]`), 수정 시 AS-IS 일급화.

### 명시적 제외 (AVOID)

*이 스킬 안에서의* 회고/임시코드 cleanup 추적, 비용 렌즈, 프론트 계약 전용 페르소나, 학습 retention 루프. (요구사항 정의 스킬이 전과정 스킬로 번지는 것 방지). 코드 리뷰·테스트 리스트 감사·상세 DB 설계는 기존 도구로 핸드오프 (반기계적 → 제네릭이 맞는 도구, DRY).

> **범위 정정 (2026-08)**: AVOID 의 "회고"는 *defining-requirements 가 회고까지 떠안는 것*을 막는 조항이지 suite 전체에서 되먹임을 빼는 조항이 아니었다. 그런데 그렇게 읽히면서 13단계 역주입이 문서 3곳(`checklist.md` 머리말·`auditing/SKILL.md` 절차 5·§7 한계)에 선언만 되고 실행 입구가 없는 채로 남았다 — 실측: `checklist.md` 는 생성 커밋 이후 0회 수정(목록외 실효 2건에도). 되먹임은 스킬이 아니라 **큐**로 복원한다 (`LIFECYCLE.md` §13 역주입 루프). 임시코드·flag cleanup *추적*은 여전히 프로젝트 몫(AVOID 유지).

brainstorming 에서도 어시스턴트 추천("추천안 먼저")·spec-review 루프(≤5회 수렴)는 **의도적 비이식** — 전자는 §1(사용자 주도·체화) 위반, 후자는 `PRINCIPLES.md` §8(종료 검수 1회 bounded)이 더 나은 철학으로 이미 점유(LLM 리뷰어는 라운드마다 새 지적 → 무한 수렴은 false positive에 끌려다님). 가져온 건 형태/분해·시각 companion 2개뿐 (§2 graft 행).

## 4. 스위트 위치 + 핸드오프

```
[아이디어 탐색] → [defining-requirements](#1) → [designing-data-model](#2) →
[designing-code](#3) → [구현 계획] → [TDD] → [코드 리뷰]
```

옆 핸드오프: 상세 스키마=DB 설계 도구, 테스트 리스트=리스트 검증 도구, 구현 후=코드 리뷰. **타깃의 실제 이름/경로는 프로젝트 프로필(`handoffs.*`)에서만** 가져오고 존재 확인 후 제안한다.

## 5. 후속 스킬 메모 (정식 설계는 각자 사이클)

- **#2 `designing-data-model`** ✅ 빌드됨: 상세 스키마/인덱스. 자체 페르소나 [REV][DBA][DATA][SRE][SEC]로 **portable** (선행 DB 페르소나 정의를 genericize). 프로젝트에 더 풍부한 DB 도구 있으면 프로필 `handoffs.db_design`로 *선택 참조*(통째 deferral ❌ — 다른 프로젝트서 빔). 프로젝트 자체 스키마 *내용*은 그 repo에 남김.
- **#3 `designing-code`** ✅ **v1(미검증)**: 모듈·코드 설계 — 자체 `[ARCH][REV][SEC]` 페르소나. genericize 원본 없이 맨땅 저술 → *첫 코드설계 무거운 피처서 dogfood로 검증·수정* 예정(build-now-refine-later). writing-plans(구현 *계획*)와 다른 단계.
- **`validating-test-lists`** ✅ 이동됨: 테스트 리스트업(6b) — 기존 standalone 개인 스킬을 플러그인으로 이동(portable). suite 정체성이 "설계 핑퐁"→"비-superpowers 라이프사이클 도구 모음"으로 넓어짐.
- **`auditing-production-risks`** ✅ v1(미검증): 구현 후(8.5) 프로덕션 리스크 감사 — 검증된 패턴 2종의 재적용(판정표=validating-test-lists, 단발 감사 에이전트=테스트 감사 도구 류)이라 신규 저술은 체크리스트 콘텐츠뿐. 체크리스트는 설계 페르소나가 잡았어야 할 것의 **2차 안전망** — 카테고리별 위반 반복은 설계 질문뱅크 보강 신호. *부하테스트는 스킬로 안 만듦*: NFR 게이트 필드(1차) + 체크리스트 재판정(2차) + 라이프사이클 10.5 조건부 행만 — 핑퐁 실천 0회라 추출 규율(2회+ 자연 수행) 미충족, 충족 시 추출.
- **형태/분해 게이트 (brainstorming graft)** ✅ `defining-requirements` 게이트0에 fold(2026-06-23): 입력이 단일 본체냐 다중 서브시스템이냐 판별 → 분해 → 첫 본체 선택. 기존 스코프 슬라이싱(본체 *내* 조각 분리)의 상류 레벨1 — 둘은 같은 형태 축의 상·하류라 텍스트로 명시 구분. [PM] 질문뱅크에 backstop 1줄. *추출 트리거*: dogfood에서 형태/분해가 반복적으로 자기 세션·산출 문서를 요구(2회+)하면 독립 stage-0 스킬 `shaping-ideas`로 추출(LIFECYCLE row 0 신설 + 게이트0은 shaped 입력 가정으로 환원) — 부하테스트 보류와 동일 "2회+ 자연 수행 시 추출" 규율. 현재는 fold가 right-size(별도 세션·문서 불필요).

## 6. dogfood 로그 (검증 이력)

> build-now-refine-later 전략의 **검증 상태 단일 출처**. 스킬을 실제 피처에 돌릴 때마다 1줄 추가 — cross-machine 으로 "뭐가 검증됐나"를 repo 안에서 본다. (프로젝트명은 익명 — 이 문서 관례)
>
> **익명화 규칙 (공개 repo)**: 프로젝트/회사 실명 금지. 반영 칼럼은 이 repo 커밋 SHA만 적는다 — 반영처가 외부(프로젝트) 문서라 SHA가 없으면 `(프로젝트 문서)` 로만 표기. 위반은 CI `masking-guard` 가 차단한다 (denylist 는 repo secret `MASKING_DENYLIST`).
>
> **무거움 tier 행 기록 의무**: 7.5 스케일 라운드 offer 의 수행 여부를 `7.5: 수행/skip/누락` 한 토큰으로 함께 적는다 — §7 "ⓒ 실행자 누락" 거취 트리거(2회)의 판정 입력이 이 칸이라, 안 적으면 그 트리거는 영영 판정 불가다 (실례: F17 무거움 2행이 수행 여부 미기록이라 카운트에 못 들어감).

| 날짜 | 스킬 | 피처 (tier) | 발견 / 결과 | 반영 |
|---|---|---|---|---|
| 2026-06-06 | defining-requirements | 카드 재평가 — 유료 재화 과금 (무거움) | 풀 6종 핑퐁 정상 — 요구사항 문서 산출 + 프로필 crosscutting 누적. 같은 날 보강: 본질 도출·right-size·피드백 모드·용어·챕터 자기점검 | 6355c72 ~ 6e0579f |
| 2026-06-06 | designing-data-model | 재평가 데이터 모델 — 회차(attempt) 스키마 (무거움) | 수정 피처에서 현행 스키마·쿼리 조사가 질문 한 줄로는 부족 → AS-IS 조사를 일급 step 으로 승격 | d956c59 |
| 2026-06-07 | designing-code | 재평가 (light 경로 — 기존 모듈 수정 위주) | right-size 경고 정상 작동(skip 권고 발동). 구현 plan *이후* 실행 시 file-structure 중복 발견 → 순서 경고 추가. **heavy 코어(새 모듈)는 여전히 미검증** | 1d27063 |
| 2026-06-10 | auditing-production-risks | 카드 재화 키 리네임 + 가격 조정 (재화 접촉) | 첫 실전 — 8카테고리 전 판정·CRITICAL 0. **목록외 WARN 1건**(문서-구현 drift) → 문서 정정 PR로 해소 — 목록외 탈출구 첫 실효 | (프로젝트 문서) |
| 2026-06-11 | auditing-production-risks | 카드 점수 개봉 게이트 (상태 도입) | CRITICAL 0. WARN 1건(배정 루프 stale 엔트리 분기 — 무손상·회전만) → 항목별 검증 후 **기각, 후속 보강 후보로 기록** | (프로젝트 문서) |
| 2026-06-11 | defining-requirements §8 종료 독립 검수 | 카드 점수 개봉 게이트 (무거움-경량) | **ⓐ 실제 누락 2건+**: AC에만 있고 본문에 없는 요구(self-contained 위반), API 계약 미확정 ↔ 클라 선배포 모순, 롤백 스크립트 미등재, 후속 트리거의 관측 수단 공백 — 전부 문서 반영 | (프로젝트 문서) |
| 2026-06-11 | defining-requirements §10 결정 스팟 반박 | 카드 점수 개봉 게이트 — P2 정책 + status enum 삽입 | **ⓐ 결정 보강 유발**: 롤백 lossy → 백업 덤프 추가, push형 부수효과 전수 점검 강제(검증 결과 닫힘), 기공개 자산 실종 케이스 문서화. CRITICAL 주장 2건은 코드 검증으로 기각(필터 포함형·알림 payload 무점수) — 항목별 검증 필수 재확인 | (프로젝트 문서) |
| 2026-06-12 | defining-requirements (중간 tier 경로) + §8 종료 독립 검수 | 평가 진행 수 노출 — 읽기 전용 응답 필드 (중간) | 중간 tier 경로([PM][REV][QA]+[TECH] 약식+[SEC] 트리거) 정상. §8 검수: **ⓐ 없음 — 지적 0건(전 섹션 통과)**. 단 ⓑ·ⓒ 노이즈도 0이라 "억지 지적 금지"는 작동 | (프로젝트 문서) |
| 2026-06-12 | defining-requirements (중간 tier + [TECH][OPS] 스팟) + §8 종료 독립 검수 | 카드 오픈 before/after — 재평가 전후 점수·컬러 표시 (중간) | §8 검수 8건 → **ⓐ 4.5건 수용**(fallback 재계산 규칙 미명시·결손 정의·회차 출처 등 self-contained 갭 + 권한 회귀 AC). 기각 3건 = 게이트 N/A 오독·AC 실현성 오판(입력 선택 ≠ seam-seed)·E2E 과잉 — 항목별 검증 필수 재확인 | (프로젝트 문서) |
| 2026-06-12 | auditing-production-risks | 카드 오픈 before/after (응답 필드) | CRITICAL 0·WARN 0. 목록외 = 기존 부채(스키마 주석 가격 표기) 기록. 별도 목록외 1건은 이슈화 → fail-first 재현에서 실 증상이 **완료 트랜잭션 영구 실패(CRITICAL급)** 로 판명·수정 — 목록외 실효 2호 + 교훈 "목록외는 이슈화 전 도달 가능성 독립 검증" | (프로젝트 문서) |
| 2026-06-12 | auditing-production-risks | 위 후속 — 회차 산출 쌍 추가 | CRITICAL 0 — 4회 연속 무위반, 재감사 루프 미진입 | (프로젝트 문서) |
| 2026-06-12 | (운영 분석 — 스킬 실행 아님) | 최근 3피처 파이프라인 실행 시간 트랜스크립트 실측 분해 | 요구사항 단계 비용이 피처 폭에 비례(무거움 tier 108분 vs 중간 tier 28분) + 자기 완결 조각이 본체 tier 를 끌어올림 → **게이트 0 스코프 슬라이싱** 추가. 빈 인자 실행·동일 문서 재검증 마찰 → **validating-test-lists 절차 0 입력 확정 게이트** 추가 | d7e022b |
| 2026-06-15 | defining-requirements (무거움) + §8 종료 독립 검수 | 어드민 일별 지표 사전집계 롤업 — tall 컬렉션 대체+전체백필+구컬렉션 은퇴 (무거움, 읽기전용) | 풀 6종 정상(kill-gate가 resume의 "성능" 명분을 코드로 무효화 → "과거 window 조회+동결"로 Why 교정). §8 5지적 → **ⓐ 2건 수용**: 부분-굽기 재개 AC 누락 / 미결 "현 getDau() 미러링" 가이드가 틀림 — 항목별 검증 중 getDau가 **UTC 버킷팅+null포함**임을 코드 확인, 미러링=KST 정책 위반이라 "롤업이 교정"으로 정정 + ⓒ 경미 1건(동결 강도 한정조건). **기각 2건**: type=CONSUME 필터가 reward purpose 자동 배제(차원합 AC 성립)·라인참조 오판(`19행` 실제 정확). 교훈 = 항목별 검증이 리뷰어 사실오류 + 리뷰어보다 깊은 버그(getDau UTC) 둘 다 잡음 | (프로젝트 문서) |
| 2026-06-19 | defining-requirements (중간 tier) + §8 종료 독립 검수 | 좋아요 알림 푸시 딥링크 — 대상 id payload 적재, 사후 정의 (중간, 서버 선구현) | 중간 tier 경로([PM][REV][QA]+[TECH]+[SEC]/[OPS] 경량) 정상. 무거움 결정(E1a 서버 suppress 폐기=서비스 경계 "자기 collection만" 보존, E1b 클라 fallback 대체)이 본 핑퐁서 닫힘 — 사용자가 경계 원칙 제기. §8 검수 2지적 → **ⓐ 2건 수용**: payload `senderId` 익명성(=ADMIN, 좋아요 주체 아님) 정책 미선언(PII 경계) / push-only라 CS 사후 payload 조회 불가인데 "검증 가능" 모순 → 재현·코드결정성으로 정정. 항목별 코드 검증 후 수용(senderId payload 적재·saveNotification=false 확인) | (프로젝트 문서) |
| 2026-06-19 | auditing-production-risks | 좋아요 알림 푸시 딥링크 — profileId 적재 (큐+FCM 접촉) | 8 카테고리 전수 판정 · **CRITICAL 0 · 위반 0 · 목록외 0**. expand-only(식별자 1개 운반) — backward-compat(`profileId?` optional+삼항)·FCM string 규격(`toFcmData` undefined 생략)·IDOR/익명성(수신자=카드 소유자·liker=ADMIN) 전부 코드 인용으로 통과. 단발 무위반 지속 → 재감사 루프 여전히 미진입 | (프로젝트 문서) |
| 2026-06-26 | designing-data-model + §8 종료 독립 검수 | 여행 핀 지도 — Trip·Pin·Photo D1/SQLite 스키마 (greenfield, 중간) | **designing-data-model 첫 §8 검수**(기존 §8 dogfood는 전부 defining-requirements). 8섹션 전 통과·CRITICAL 0. **ⓐ 1건**: 좌표는 `CHECK(lat BETWEEN…)` 가드인데 날짜 형식/순서 검증이 비대칭(주석만) — self-contained 갭. [REV] 자기점검이 좌표 CHECK는 잡고 날짜는 놓쳐 외부 시선이 비대칭 포착. + ⓒ 1건(사진 serving/read 경로 = designing-code 영역 경계). | (프로젝트 문서) |
| 2026-07-09 | defining-requirements (중간 tier + [SEC] 확정 발동) + §8 종료 독립 검수 | 피드 게시물 등록 — 신규 모듈·이미지 key-only 저장 (중간) | 중간 경로 정상 — 외부 업로드 Worker 소스 실물 grounding으로 key regex 도출, robustness 기각 2건(멱등키·rate limit 강화 — 강제조건 불성립, 메인이 낸 권고를 사용자 반문 후 철회 1건 포함). §8 검수 8지적 → **ⓐ 4건+ 수용**(하류 커서 축 필드(createdAt) 전면 침묵 — 사용자 확정 제약에 있었는데 [REV] 누락 / 상류 문서 reconciliation "완료" 주장 대비 부분 반영 2건 / 기각한 검증 레벨의 수용 비용 포인터 공백) + 경미 3건(문구 정확도·trim↔길이 순서 미정·사후판정 치환 주체 미바인딩). 지적 1건의 실질(외부 Worker가 클라 fileName을 key에 쓰는지)은 소스 실물로 해소 — 검수자는 접근 불가 시 "판정 불가"로 정확히 던짐(§11 부재 단정 금지 준수 사례). 항목별 독립 검증 후 반영 | (프로젝트 문서) |
| 2026-07-09 | designing-data-model + §8 종료 독립 검수 | 피드 게시물 등록 — 신규 컬렉션 스키마·인덱스 (중간) | 열린 결정 5건 중 3건이 관례 grounding(공통 스키마 옵션·id 타입·서브도큐먼트 선례 실측)으로 사실상 소거 — grounding이 결정 비용을 줄이는 경로 확인. "자체 소비 인덱스 0개" 표면화로 인덱스 결정을 정확성→소유·조율 문제로 재프레임. §8 검수 2지적 → **ⓐ 2건 수용**: 계정 삭제 연동 인계 슬롯 부재(본문 3곳이 소비자로 호명하는데 핸드오프에 무소유 — dangling 참조, [REV] 자기점검이 놓침) / 템플릿 "기존 스키마 문서 시스템 반영" 괄호 항목 무응답(빈칸 방치 금지 위반). CRITICAL 0·본문 기술 판단 오류 0 — 검수자가 문서 인용 코드 사실 12건+(채택률 숫자 포함)을 전건 실코드 대조. 항목별 독립 검증 후 반영 | (프로젝트 문서) |
| 2026-07-10 | defining-requirements (무거움 풀 6종) + §10 결정 스팟 반박 | 친구→팔로우 전환 — 관계 모델 대체+데이터 마이그레이션+채팅 요청함 게이트+소비처 전환 (무거움) | 게이트0 소비처 전수 스윕이 입력 전제("소비처 3곳")를 7계열로 확장 — grounding이 요구사항 폭 자체를 교정. §10 반박 7건 → **ⓐ 수용 6/기각 1**: 배포 순서 재편(클라 대면 전환을 강제업데이트 *뒤*로 — 전환 단계 분리), 백필 차단 anti-join(코드 CONFIRMED — pending-후-차단 쌍이 팔로워로 부활하는 구멍), 창 기간 삭제 방향 배선(멱등 upsert는 삭제 표현 불가), 채팅 멤버 상태 모호성 1회 정리("소급 0" 전제 반증), 분류기 입력 발견(표시 항목 제거 ≠ 재설계), 알림 삭제 whitelist. 반박 1건 절반 반증(산 기능이 그 알림 타입 생성한다는 주장 — 코드 대조 기각)하며 **인접 실구멍**(i18n 키 공유 소비) 발견 — 항목별 검증이 리뷰어 오류와 더 깊은 구멍 둘 다 잡는 패턴 재확인. 기각 1건(presence 상한)은 사용자 재고 후 기각 — 스케일 트리거로 문서화 | (프로젝트 문서) |
| 2026-07-10 | defining-requirements §8 종료 독립 검수 | 친구→팔로우 전환 (무거움) | 검수자가 문서의 코드 계약 주장 ~20건(file:line)을 전건 실코드 대조 — **전건 정확** 판정(§11 grounding 실효). 5지적 → **ⓐ 1건 수용**: 본문이 "§10 반박 N"을 핵심 근거로 8회 인용하는데 **반박 기록 섹션이 문서에 미편입**(dangling 참조 — 수용/기각 tally 재구성 불가, self-contained 실패. [REV] 종료 패스가 놓침) → §10 기록 섹션 신설로 해소. + 중간 1 수용(차단 런타임 edge 삭제·카운트 불변식의 번호 AC 승격 — blanket "예외표 각 행이 AC" 선언 뒤에 숨어 있던 갭) + 낮음 2 수용(라인 참조가 문서 소속 브랜치 기준 stale — 리팩터 유동 라인은 메서드명 기재로 / 미결 행이 "확정 정책" 표에 혼재) + **기각 1**(사후판정 절대날짜 — 앵커(contract 배포일) 미확정이라 릴리즈 기록 등재 시점 치환 바인딩이 관례 정합, 검수자도 "수용 가능" 판정). 항목별 독립 검증 후 반영 | (프로젝트 문서) |
| 2026-08-06 | defining-requirements (무거움 풀 6종) + §8 종료 독립 검수 | F11 댓글 색상 — 기존 400 계약 뒤집기 + 색별 차등가 루비 차감 (무거움, 재화) | 검수자가 문서의 `file:line` 주장 30건+ 전건 대조 — 어긋난 건 줄 앵커 drift 4건뿐. 10지적 **전건 CONFIRMED·기각 0**, 그중 **ⓐ 4건**: (1) §5 가 "댓글↔이력을 requestId 로 잇는다"고 적었으나 `rubyhistories` 에 그 필드가 없고 `transactionId` 는 ObjectId setter — *검증 불가능한 것을 검증 가능하게 적은* 템플릿 §5 금지 패턴을 [REV] 종료 패스가 놓침 (2) AC 가 미지정 색을 "저장값 white"로 적어 신규 무색 댓글이 전부 상류 피처(F10)의 키-생략 계약을 깨게 되어 있었음 — 확정 문면끼리는 무모순이라 자기점검으로 구조적 미검출 (3) 멱등 선조회와 기존 가드의 순서 미정으로 재전송 관찰 결과가 구현마다 갈림 (4) 배포 창(코드 후·시드 전)의 조회 API 동작 미정의. 나머지 6건은 문서 내부 불일치·AC 1:1 누락·앵커 drift. **§8 체인 ⓐ 6연속** — 제거 트리거 미충족 | (프로젝트 문서) |
| 2026-08-06 | defining-requirements §10 결정 스팟 반박 | F11 댓글 색상 (무거움, 재화) | 반박 6건 → **반박 성립 1 · 조건부 2 · 반박 실패 3**(억지 반박 없음 — "결정이 옳다"를 근거와 함께 3건 반환). **ⓐ**: 성립 1건이 확정 1건을 교체했다 — 메인이 "0원 가격은 `$inc:-0` 이 modifiedCount 0 을 내 잔액부족으로 오인 실패"라고 *추론*한 것을 반박자가 인메모리 Mongo 실측으로 반증(`-0` 은 modified=1, `+0` 만 0). 실제 리스크는 정반대(그 색이 **에러 없이 무료로 팔린다** — 매출 누락·무관측)여서 방어를 발사 시점 예외 → 장전 시점 거부(admin API 가 0 거부)로 이동. 조건부 2건에서 실코드 유틸(`runWithTransientRetry`) 존재를 근거로 E11000/WriteConflict 미구분 구멍 + mongoose undefined 필터 함정을 제시 → 확정 2건 보강. **검증자가 메인의 미실측 추론을 실측으로 뒤집은 첫 사례** — §11 grounding 이 메인에도 걸린다는 실증(라이브러리·엔진 동작은 선례 0이면 실측 전 단정 금지) | (프로젝트 문서) |
| 2026-08-14 | defining-requirements (무거움 풀 6종) + §10 결정 스팟 반박 | F17 추천 신호 로깅 — 피드 서빙 로그·반응 이벤트 신설 + 읽기 경로 "부작용 없음" 계약 재서술 (무거움) | 반박 9건 → **성립·부분 6 / 실패 3**(실패 3건도 근거 동반 반환 — 억지 반박 없음). **ⓐ 결정 변경 2건 유발**: ① requestId 계약을 impression 단독 → 반응 3종(like·detail)으로 확장 — "구버전 앱은 임시 강등인데 라벨 쪽은 설계상 영구 강등"이라는 비대칭 논증 ② 탈퇴 정리를 in-txn deleteMany → 커밋 후 best-effort 로 전환 — txn 매니저 무재시도 + 유저축 인덱스 누락이 무증상인 구조를 코드 실측으로 입증. 부수 = dedup(momentId) 재정의 없인 원소별 에코 불완전 / impression 착지 미정이 타 결정 3건과 상호 모순 — 요구사항 단계 구멍 2건 선폐쇄. **§10 체인 ⓐ 4연속** | (프로젝트 문서) |
| 2026-08-14 | defining-requirements §8 종료 독립 검수 | F17 추천 신호 로깅 (무거움) | 7지적 전건 수용·기각 0 — 문서 인용 코드 전수 실코드 대조(확인불가 0). **ⓐ 3건**: [PM] 확정값(댓글·신고=학습 입력 포함)이 문서 작성 중 누락 / dedup 재정의의 상류 무효화 3곳 reconciliation 누락(선행 plan 의 bulkWrite index-align **전제** 충돌 포함 — 문서 낡음이 아니라 구현 리스크) / 헤더의 "Q1~Q7 폐쇄" 주장 대비 Q2 본문 무응답. + 경미 4건(분모 카운터 관측창 조건화·best-effort "선례"가 실은 bare await 라는 성격 정정·알람 무룰 결정 명시·impression 착지 컬렉션 보존·탈퇴 상속 규칙). **§8 체인 ⓐ 7연속** — 제거 트리거 미충족 | (프로젝트 문서) |
| 2026-08-15 | designing-data-model (무거움 풀 5종) + §8 종료 독립 검수 | F17 추천 신호 로깅 — 신호 컬렉션 3종·인덱스·TTL (무거움) | 6지적 중 4수용·2기각. **ⓐ 3건**: 캐시 지분 산술이 두 컬렉션 문서 수를 단일 인덱스에 곱해 1.7~2.4배 과장(같은 오류가 레지스트리에도 복제됨) / impression 행수 가정(서빙의 70%)이 미기재라 볼륨 추정 재현 불가 — 출시 후 실측 괴리를 해석할 기준선 상실 / 세션 중 완료한 레지스트리 등재가 문서엔 미완 액션으로 남아 하류 중복 수행 유발. + 상류 요구사항 "컬렉션 2종"이 결정 1(3종)로 무효화됐는데 reconciliation 이 **미배정**(§9 위반) — 지적 후 4곳 정정. 기각 2건: 재발 클래스 날짜(레지스트리에 발견일·생성일이 각각 있어 인용이 맞음), 표 형식 편차(partial/covered 부재 피처라 의도적 대체). **designing-data-model §8 3회차 = ⓐ** — 제거 트리거 미충족 | (프로젝트 문서) |
| 2026-08-15 | designing-data-model §10 결정 스팟 반박 | F17 추천 신호 로깅 (무거움, 최대 볼륨 컬렉션) | 8반박 중 4수용·2부분수용·2기각. **ⓐ — 결정 1건 번복**: TTL 14일 → **28일**. 반박자가 "디스크 위험은 트래픽 도래 후에만 실재하고 지금 트래픽은 실질 0"을 지적 → 14일/28일의 디스크 차이는 *14일차 이후*에만 나타나고 그 시점엔 이미 실측이 있으므로, 원 결정이 위험의 **시점**을 잘못 봤다는 것이 확인됨(단축은 무손실·연장은 소급 불가라는 비대칭이 되살아남). 반박자의 90일 제안은 기각(여유의 61% + "주 단위" 문면 위반). 그 밖 ⓐ: 조인 소스(`momentcomments`)의 status 전이로 학습 신호가 소실/오염되는 축 + 탈퇴 파기 정책이 소스별로 갈리는 축, 유저 슬라이스 루프의 집합 소스 미정의. **기각 2건은 §10 규율의 구조적 대가** — 확정 정책(requestId 에코·탈퇴 삭제 자리)을 전달하지 않으므로 반박자가 그 부재를 전제로 논증. 닻 제거의 값이라 규율은 유지하되, 판정 시 "컨텍스트 부재로 인한 반박"을 별도 분류해야 함. **§10 체인 ⓐ 3연속** — 제거 트리거 미충족 | (프로젝트 문서) |

### 거취 트리거 보드 (카운트 단일 출처)

> 아래 산문(검증 대기)에 흩어져 있던 거취 트리거를 한 표로 모은다. **카운트는 이 보드가 단일 출처** — 산문은 각 항목의 근거·처방 상세만 담는다. 신설 이유: 트리거가 100행 산문에 묻혀 있어 §9 승격 트리거가 **발동(2회 충족)됐는데도 두 세션이 지나치는** 실측(2026-08-16)이 났다 — 카운트를 스캔 가능한 자리로 옮기지 않으면 재발한다. dogfood 로그를 적을 때 해당 행 카운트를 함께 갱신한다.

| 항목 | 트리거 | 카운트 | 상태 |
|---|---|---|---|
| seeded-defect 캘리브레이션 (감사) | 위반 0 통산 6런 도달 시 1회 수행 | 5/6 | 다음 감사 대기 (마지막 감사 2026-06-19 — 두 달 공백 자체도 점검 대상) |
| §8 제거 (defining-requirements) | 2회 연속 ⓐ 없음 | 0/2 (ⓐ 7연속) | 유지 |
| §8 제거 (designing-data-model) | 〃 | 0/2 (ⓐ 3연속) | 유지 |
| §10 제거 (defining-requirements) | 2회 연속 ⓐ 없음 | 0/2 (ⓐ 4연속) | 유지 |
| §10 제거 (designing-data-model) | 〃 | 0/2 (1회차 ⓐ) | 유지 |
| 게이트 0 스코프 슬라이싱 제거 | 2회 연속 실값 없음 | 0/2 | 유지 |
| validating 절차 0 변경분 조항 제거 | 오판 실측 1회 | 0/1 | 유지 |
| §8 기각 패턴 역주입 | 기각 3패턴 재발 (N=2) | 1/2 | 보류 |
| **§9 reconciliation 승격** | 누락 실측 2회 | **2/2 — 발동** | **집행됨 (2026-08-16 → PRINCIPLES §4 종료 패스 상시 항목)** |
| solo observe-only 폴백 축소 | 오너 있는 피처 남용 실측 | 0/1 | 관측 중 |
| 7.5 offer 위치 이동 (ⓒ 실행자 누락) | 실행자 누락 2회 | 1/2 (+판정 불가 2건 — F17 무거움 2행 수행 여부 미기록) | 로그 머리말에 기록 칸 신설 (2026-08-16) |
| 스케일 학습 라운드 ⓐⓑ 거취 | dogfood 2회 관측 | 0/2 | 관측 중 |
| 형태/분해 `shaping-ideas` 추출 | 자기 세션·문서 요구 2회+ | 0/2 | fold 유지 |
| 부하테스트 스킬 추출 | 핑퐁 자연 수행 2회+ | 0/2 | 보류 |
| 역주입 큐 회고 스킬 추출 | 3건+ 적체 또는 2회+ 방치 | 0건 (큐 파일 자체가 미생성) | 큐 유지 — 원인은 방치가 아니라 **적기 미배선**: 큐 입력 3종 중 §8/§10 반복 지적에 append 지시가 없었다 → 2026-08-16 배선 (PRINCIPLES §8/§10) |

검증 대기: `auditing-production-risks` **재감사 루프**(위반 수용→수정→재검 1회) — 단발 감사 경로는 5회 검증(전 런 CRITICAL 0·목록외 실효 2건)됐으나 무위반이라 루프 미진입, 첫 위반 감사에서 자연 검증. **단 "위반 0 연속"은 두 가지로 읽힌다 — 설계 페르소나가 실제로 잘 막았거나(의도한 결과), 감사가 무르거나(false negative). 지금 이 둘을 가를 수단이 없다.** 다음 감사에서도 위반 0 이면(= 통산 6런) **seeded-defect 캘리브레이션 1회**로 판별한다: 체크리스트 대표 항목의 방어를 일부러 제거한 변경(멱등키 제거·소유자 검증 제거·타임아웃 제거 중 1개)을 만들어 평소와 같은 프롬프트로 dispatch → **검출 여부 확인 후 즉시 원복**. 검출하면 "위반 0 = 설계가 막았다"가 근거를 얻고(그리고 위반 리포트가 나오므로 재감사 루프도 같이 검증된다), 못 잡으면 감사자 모델 tier 를 먼저 올린다(§6 대응과 동일 — 체크리스트 수술보다 모델이 싼 첫 레버). 결과는 dogfood 로그 1줄. 테스트 가드레일의 mutation(변형 → RED 확인 → 원복)과 같은 규율을 감사자에 적용하는 것 — *검증자를 검증할 oracle 이 없으면 검증자는 무한히 무를 수 있다*. §8: 1회차 ⓐ → 2회차 ⓐ 없음 → 3회차(카드 오픈 before/after) **ⓐ 재발** → 4회차(일별 롤업) **ⓐ**(부분-굽기 AC·getDau 미러링 오류) → 5회차(좋아요 딥링크, 중간 tier) **ⓐ**(senderId 익명성 미선언·CS 사후조회 모순) — 제거 트리거(2회 *연속* ⓐ 없음) 여전히 미충족, §8 유지. (defining-requirements §8 체인.) **designing-data-model 첫 §8**(2026-06-26, 여행 핀 지도 중간 tier) = **ⓐ**(좌표 CHECK는 잡고 날짜 검증 비대칭 놓침) → 2회차(2026-07-09, 피드 게시물 스키마) **ⓐ**(계정 삭제 연동 인계 dangling) — 공유 §8 메커니즘이 타 설계 스킬에서도 ⓐ 실효 확인. §10: 1회차 ⓐ → 2회차(친구→팔로우 전환, 무거움) **ⓐ**(배포 순서 재편+백필 anti-join 등 6건 수용) — 제거 트리거(2회 연속 ⓐ 없음) 미충족, 유지. **게이트 0 스코프 슬라이싱**(d7e022b, N=1 선반영): 2회 연속 실값 없음(자를 게 없는 피처에서 라운드만 소모, 분리 제안 유효 0회)이면 제거 — 단 판정 정보 부족(AS-IS 조사 *전* 판정이라 파일·배포 독립을 모름) 실측 시는 제거가 아니라 "AS-IS 후 재방문" 한 줄 보강. **validating 절차 0**(d7e022b): 변경분 판정이 피처 정의 변경을 놓치는 오판 실측 시 변경분 조항만 제거(대상 질문 조항은 마찰 실측 근거 있어 유지). **§8 기각 패턴 역주입**: 보류 — 기각 3패턴(게이트 N/A 오독·입력 선택≠seam-seed 혼동·E2E 과잉, dogfood 4행차 기록)이 재발(N=2)하면 §8에 "지적 전 확인 절차" 문구로 주입 (억제 문구 금지 — 진짜 누락 은폐·거취 실험 오염 방지). **§11 grounding + §9 reconciliation + solo 사후판정 폴백**(2026-06-23 추가, n=0 — 한 무거움 tier 피처의 superpowers-only vs suite+grounded-plan 비교에서 도출): §11·§8/§10 grounding 은 *불변식*(코드 대조 없이 단정 금지)이라 거취 아닌 **보강** 대상(검증자가 재누락하면 본문 강화). reconciliation·solo 폴백은 *정책*이라 거취 트리거 부착 — 다음 2~3 피처서 발동/유용 관측: (a) reconciliation = 상류 무효화가 *있었는데도* 누락돼 드리프트 재발하거나 무효화 상황이 2회+ 와도 한 번도 안 적었으면(작성자 인지 실패) §9 를 [REV] 종료 패스의 압박 질문으로 승격 — **2026-08-16 발동·집행됨**(F17 요구사항·데이터모델 연속 2회 누락 실측 → PRINCIPLES §4 종료 패스 상시 항목화); (b) solo 폴백 = 오너 있는 피처까지 observe-only 로 임계를 회피(남용)하면 폴백 조건을 '오너 부재'로 더 좁힌다. 발동·유용은 dogfood 로그 1줄로.

### 검증자 누락 실측 판정 (감사·검수 공통)

검증자(8.5 감사·§8 종료 검수·테스트 리뷰어 등)가 "위반을 놓쳤다"는 검증 시점엔 관측 불가능 — **사후 채널 역추적**으로만 판정한다. 누락 1건 = 아래 3조건 전부 충족:

1. **시점**: 결함의 도입 커밋이 그 검증 런이 본 diff 안에 있다 (git blame/log 로 판정 — 검증 *이후* 커밋이 만든 결함은 제외).
2. **범위**: 그 검증자의 체크리스트/기준 범위 안이고, 검증 시점 기준 **CRITICAL 로 분류됐어야 할** 결함. 범위 밖이면 누락이 아니라 **목록외 갭** → checklist 역주입 회고 루프 대상 (모델 탓 아님). WARN 급은 기록만, 카운트 제외.
3. **발견 채널**: 사후 발견 — ① 운영 모니터링(에러 트래커·DLQ·정합성 스크립트) ② PR 리뷰·코드리뷰·CI ③ 후속 작업 중 발견.

기록: dogfood 로그에 1행 (발견 채널 · 카테고리 · 어느 검증 런의 누락인지 역참조). **대응은 N=1 즉시**: 누락 1건 실측 → **그 검증자의 모델을 상위 tier 로 교체**해 운용한다 (검증 품질은 추론 깊이에 민감 — 모델 교체가 가장 싼 첫 레버, 체크리스트 수술보다 먼저). 상위 모델로도 같은 검증자가 재누락하면 모델이 아니라 **체크리스트/프롬프트 결함으로 재분류** — 본문(checklist·기준)을 보강한다. tier 분담의 전제는 `LIFECYCLE.md` §모델 운용.

## 7. 한계 / 트레이드오프

- 떠먹이기 드리프트: 협업 모드가 각도를 너무 쉽게 주면 학습이 죽음 → "막혔을 때만" 보정으로 방어하나 해석 여지 남음.
- 트리거 누락: `[SEC]`/`[OPS]` 키워드 기반이라 비전형 표현 놓칠 수 있음 → 챕터 자기점검의 트리거 점검(1차) + 종료 완성도 패스(2차)로 방어 (`PRINCIPLES.md` §4). 단 1차엔 **순환성**이 남음 — 트리거를 놓친 컨텍스트가 자기 누락을 점검. 챕터마다 서브에이전트 점검은 기각(2026-06-10): 감사 대상이 외부화 산출물이 아니라 대화라서(직렬화하면 독립성 희석, 안 하면 일반론 노이즈) + 핑퐁 리듬 파괴. **종료 독립 검수 설치**(2026-06-10, `PRINCIPLES.md` §8, additive — [REV] 패스 유지): 외부화된 완성 문서 대상이라 8.5와 동형, self-contained 검증 겸용. 당초 "8.5 입증 후 이식"이었으나 선설치로 전환 — 메커니즘은 8.5에 설계 완료(만들 내용이 이미 있음) + 다음 피처의 설계가 구현(8.5 dogfood)보다 먼저 와 verify-then-port 는 한 사이클 손해 + 가역적(몇 줄). 대신 **제거 트리거**: dogfood 2회 연속 실효 없음(ⓑ[REV] 중복·ⓒ일반론뿐, ⓐ실제 누락 0)이면 제거.
- 프로필 미작성 시 마찰: 묻기로 동작하나 매번 재질문 → 자동 생성으로 1회성화.
- **스케일 학습 모드의 두 누수**(2026-08 신설, n=0): ⓐ *스코프 크립* — "커지면 이렇게"를 탐색하다 이번 범위로 끌어오는 것. 방어 = 절차·원칙·템플릿 3곳에 "라운드를 돌아도 이번 구현은 최단순 유지" 명시, 위반 실측 시 offer 를 무거움 tier *산출 직후*(결정 동결 후)로 옮긴다. ⓑ *문서 비대* — 학습 탐색이 산출물로 새어 하류 계약의 신호 대 잡음이 무너지는 것. 방어 = "대화=탐색 / 문서=압축(택한 안 + 기각 이름·사유 한 줄)". 둘 다 dogfood 2회 관측 후 거취 판정 — 라운드가 매번 skip 되거나(offer 무용) 결정 변경·시야 확장 0이면 offer 를 제거하고 기록 칸만 남긴다. **ⓒ 제3의 누수 — *실행자 누락*(2026-08-06 F11, n=1)**: 무거움 tier 였는데 절차 7.5 의 offer 자체가 수행되지 않았다(사용자 skip 이 아니라 세션이 잊음). 산출물은 최소 요건을 채웠으나(§8 스케일 경로 칸 기록됨) 라운드는 열리지 않았다. **이 관측을 ⓐ/ⓑ 의 거취 트리거 입력으로 쓰면 안 된다** — "offer 가 무용"과 "offer 가 눈에 안 띔"은 정반대 처방을 부른다(전자는 제거, 후자는 배치 이동). 원인 가설: 7.5 는 긴 핑퐁의 *끝*, 종료 완성도 패스와 산출 사이에 있어 recency 를 잃고 문서 작성 관성에 밀린다(§4 goal drift 와 같은 뿌리). **트리거**: 실행자 누락 **2회**면 offer 를 *산출 직전*에서 옮긴다 — 후보 (a) 게이트0 tier 판정 직후 예약 → 산출 직전 실행 (b) [REV] 종료 완성도 패스의 점검 항목으로 편입("무거움 tier 인데 스케일 라운드를 물었나"). 배치 이동은 메커니즘 추가가 아니라 *위치* 변경이라 n=2 에서도 싸다.
- 형태 게이트 오판: 다중 피처를 '단일 본체'로 오인하면 분해를 스킵 → 게이트0 휴리스틱("독립 배포·테스트되나") 1차 + [PM] 범위 압박 backstop 2차로 방어. **미검증 — 첫 모호 피처가 dogfood**(designing-code·auditing와 동일 build-now-refine-later).
- 시각 핸드오프의 superpowers 설치 의존: `handoffs.visual`이 타 머신/프로젝트엔 없을 수 있음 → `PRINCIPLES.md` §5 존재 확인이 흡수, 미설치면 텍스트로 graceful degrade (하드 의존 X).
- 체크리스트 밖 신종 리스크: 감사(8.5)는 목록 기반이라 목록 밖은 `목록외` 자유 발견에 의존(false positive 높음) → 운영(12~13)에서 사고 발견 시 checklist.md 역주입(회고 루프)으로 수렴. **수렴은 큐를 거친다** — 발견 시점(프로젝트 repo)과 편입 시점(플러그인 repo)이 갈리므로 `<repo>/.claude/persona-feedback.md` 에 적고 플러그인 작업 때 비운다 (`LIFECYCLE.md` §13). 큐 자체의 한계: 비우는 건 여전히 사람이라, 3건+ 적체 또는 2회+ 방치가 회고 스킬 추출 트리거.