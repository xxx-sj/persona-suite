---
description: 페르소나 티키타카로 모듈/코드 구조(책임 경계·의존·패턴·에러)를 설계한다 (라이프사이클 5, v1·미검증).
---

# 코드 설계 (단계 5)

`persona-suite:designing-code` 스킬을 실행한다. 대상: "$ARGUMENTS"

**호출 범위**: 이 커맨드는 *모듈/코드 설계(단계 5)만* 실행한다. **코드 변경이 가벼우면(기존 수정 위주) 이 단계를 건너뛰고** 작업분해로 가는 게 맞다(right-size). 새 모듈/서브시스템일 때만.
**다음 단계는 자동 실행하지 않는다** — 마칠 때 작업분해(프로필 `handoffs.plan`, 예: writing-plans)로 사용자가 직접 진행.