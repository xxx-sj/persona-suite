---
description: 페르소나 티키타카로 상세 데이터 모델(스키마·인덱스·마이그·정합성)을 설계한다 (라이프사이클 4).
---

# 데이터 모델 설계 (단계 4)

`persona-suite:designing-data-model` 스킬을 실행한다. 대상: "$ARGUMENTS"

**호출 범위**: 이 커맨드는 *상세 데이터 모델 설계(단계 4)만* 실행한다. 입력으로 요구사항 문서가 필요하다(보통 `define-requirements` 산출물).
**다음 단계는 자동 실행하지 않는다** — 마칠 때 `LIFECYCLE.md` 안내에 따라 `/persona-suite:design-code`(무거우면) 또는 작업분해(프로필 `handoffs.plan`, 예: writing-plans)로 사용자가 직접 진행.